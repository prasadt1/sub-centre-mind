# Sub-Centre Mind — Requirements
# Python 3.10+

# Vector store and embeddings
faiss-cpu==1.7.4
sentence-transformers==2.2.2
huggingface_hub==0.13.4

# LangChain for RAG pipeline
langchain==0.1.0
langchain-community==0.0.10

# PDF processing
PyPDF2==3.0.1

# Hybrid retrieval (CPU-only keyword search)
rank-bm25==0.2.2

# Ollama client
ollama==0.1.0

# Testing
pytest==7.4.0
pytest-asyncio==0.21.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
requests>=2.28.0

# Demo UI (local-first, single command)
streamlit>=1.30

# Local ASR (Hindi/Marathi/English voice input — Phase G)
# Note: faster-whisper installs onnxruntime + ctranslate2; CPU-only is fine.
faster-whisper>=1.0.0

# Vision: Pillow for client-side image preprocessing in the demo
Pillow>=10.0.0
